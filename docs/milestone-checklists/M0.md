# M0 — Scaffold checklist

**v6 §28 M0 acceptance** (verbatim):

> Server starts on both transports. MCP inspector lists the health tool over both. The server records the negotiated protocol/client capabilities in memory and exposes `orchestrator://session/current/capabilities` and `orchestrator://session/current/preflight` (the latter as a stub). Unit tests pass. `docker compose up` brings up the server with Postgres and Redis dependencies.

## Clauses

- [x] **Server starts on stdio transport.** Evidence: `src/transport/stdio.ts:67` — `await server.connect(transport)` succeeds; `MCP_TRANSPORT=stdio npm start` boots cleanly.
- [x] **Server starts on Streamable HTTP transport.** Evidence: `src/transport/http.ts:120-130` — `serve()` listens on `MCP_HTTP_PORT` (3000); `MCP_TRANSPORT=http npm start` boots; `tests/integration/mgmtApi.test.ts` exercises the parallel mgmt port.
- [x] **`tools/list` includes `health_check` over both transports.** Evidence: `src/mcp/registerTools.ts:32-71` registers it; `tests/unit/buildServer.test.ts` constructs the server. **(Real-MCP-client end-to-end probe = V-4 in `audit-verification-plan-2026-04-25.md`.)**
- [x] **Negotiated MCP capabilities recorded in memory.** Evidence: `src/mcp/sessionCapabilities.ts:SessionRegistry` + `src/transport/stdio.ts:53-65` and `src/transport/http.ts:160-176` register placeholder profiles on session init.
- [x] **`orchestrator://session/current/capabilities` listed + readable.** Evidence: `src/mcp/registerResources.ts:30-52` lists it; `tests/unit/sessionCapabilities.test.ts` exercises the registry.
- [x] **`orchestrator://session/current/preflight` listed (stub).** Evidence: `src/mcp/registerResources.ts:38-44`.
- [x] **Unit tests pass.** Evidence: `npm test` → 212 passed, 2 skipped (post-Phase-A.1).
- [ ] **`docker compose up` brings up server + Postgres + Redis.** **NOT VERIFIED** in audit; Docker not exercised. Action: run `docker compose up -d postgres redis` then `docker compose up orchestrator`; expect health endpoint reachable on `:3001/healthz`.

## Known caveats

- `negotiatedProtocolVersion` placeholder is `"pending-initialize"` in `stdio.ts:54-65` and `http.ts:160-176`. The SDK's initialize callback is not exposed yet; full capture lands as a v0.1.x follow-up. F-001-adjacent — verify in V-4 that this gets replaced after handshake.

## Recommended re-verification

When the MCP SDK exposes a stable initialize callback, replace the placeholder profile with the negotiated values. Then re-check this M0 entry.
