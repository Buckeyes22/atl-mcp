# M3 — VCS provider checklist

**v6 §28 M3 acceptance** (verbatim):

> Bitbucket Cloud adapter passes contract tests. Interface surface accommodates GitHub without changes. Worktree acquire/release works on POSIX and Windows.

## Clauses

- [x] **Bitbucket Cloud adapter passes contract tests (fixtures).** `tests/integration/providers/vcs/bitbucketRestProvider.test.ts` (7 tests). All HTTP methods exercised in code have matching fixture coverage.
- [ ] **Bitbucket Cloud adapter passes against real API.** **NOT VERIFIED** — no Bitbucket Cloud credentials available. Action: V-1 in `audit-verification-plan-2026-04-25.md`.
- [x] **Interface surface accommodates GitHub without changes.** `src/providers/vcs/VcsProvider.ts` declares neutral types (no `repoSlug` baked into interface; auth abstracted via injection per ADR 0004's `BitbucketAuth` seam).
- [x] **Worktree acquire/release works on Windows.** `tests/integration/providers/vcs/worktreeManager.test.ts` (7 tests, ~3.6s on Windows).
- [ ] **Worktree acquire/release works on POSIX.** **NOT VERIFIED** — audit ran on Windows 11; no CI matrix in the repo. F-017 in audit findings. Action: F.1 in remediation plan adds `.github/workflows/ci.yml` matrix.

## Known caveats

- Webhook signature verification (`src/security/webhookSignatures.ts`) is shipped here; M10 wires actual webhook ingress endpoint.
- ADR 0004 documents app-password as the v1 default; OAuth is the same shape but deferred.

## Recommended re-verification

After V-1 (real-Bitbucket smoke) and F.1 (POSIX CI): both `[ ]` items above can flip to `[x]` with concrete evidence.
