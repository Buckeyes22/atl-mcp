# M1 — Domain model and storage checklist

**v6 §28 M1 acceptance** (verbatim):

> Create/read/update a draft project succeeds on PGlite and Postgres with `tenantId = "default"`. Trace links, readiness results, policy decisions, session profiles, ACL entries, and audit entries persist. Repository calls require `TenantScope`. Snapshot tests cover serialized blueprint and context pack. Encrypted token round-trip passes. Migration rehearsal test passes (parity between PGlite and Postgres).

## Clauses

- [x] **Create/read/update draft project on PGlite.** Evidence: `tests/integration/storage/repositories.test.ts > project repository > create → findById round-trips` + `update bumps blueprintVersion + state` (PGlite by default).
- [ ] **Same on Postgres.** **NOT VERIFIED** — Postgres parity tests skip without `DATABASE_URL`. Action: V-3 in `audit-verification-plan-2026-04-25.md` (run `docker compose up -d postgres` + set `DATABASE_URL`).
- [x] **Trace links persist.** `tests/integration/storage/repositories.test.ts > trace link repository > creates and lists by project`.
- [x] **Readiness results persist.** `tests/integration/storage/repositories.test.ts > readiness repository > inserts and reads latest`.
- [x] **Policy decisions persist.** `tests/integration/storage/repositories.test.ts > policy decision repository > inserts and retrieves`.
- [x] **MCP session profiles persist.** `tests/integration/storage/repositories.test.ts > mcp session profile repository > upserts twice and reads`.
- [x] **ACL entries persist.** `tests/integration/storage/repositories.test.ts > acl repository > upserts and finds by composite key`.
- [x] **Audit entries persist + chain validates.** `tests/integration/storage/auditRepository.test.ts` (5 tests).
- [x] **Repository calls require `TenantScope`.** Spot-check: `src/storage/repositories/projectRepository.ts` first arg is `TenantScope`. Tenant-mismatch rejection: `tests/integration/storage/repositories.test.ts > project repository > rejects tenantId mismatch`.
- [x] **Encrypted token round-trip passes.** `tests/integration/storage/tokenStore.test.ts > tokenStore with real encryption > put → get round-trips a token through actual xchacha20poly1305` + `> ciphertext at rest is not the plaintext`.
- [x] **Snapshot tests cover serialized blueprint AND context pack.** `tests/unit/domain/serialization.test.ts` (2 snapshots).
- [x] **Migration rehearsal: PGlite.** `tests/integration/storage/migrationRehearsal.test.ts > migration rehearsal: PGlite > creates the expected M1 tables` + `re-running migrations is idempotent`.
- [ ] **Migration rehearsal parity: PGlite ↔ Postgres.** **NOT VERIFIED** — same gating as the Postgres clause above. The 2 skipped tests in current run.

## Known caveats

- ADR 0001 references `src/storage/vector/` (FTS5 via better-sqlite3) for keyword index; that path lands with M7. ADR is forward-looking, not a current invariant. F-016 surfaces this; closure is an ADR-status note.

## Recommended re-verification

When `DATABASE_URL` is set in CI: 2 currently-skipped tests run; total becomes 214 passing.
