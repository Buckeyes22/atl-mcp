# Cursor addendum

Hand-authored Cursor-specific guidance. `scripts/syncAgentConfigs.ts` prepends the AGENTS.md content to this addendum and writes the combined result to `.agents/cursor.md`.

## Cursor-specific notes

- Cursor reads `.cursorrules` natively; the `.agents/cursor.md` location is for cross-host inventory.
- Refer to [`docs/partners/everything-claude-code.md`](../partners/everything-claude-code.md) for editor-host parity considerations.
