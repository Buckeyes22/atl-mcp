# Copilot addendum

Hand-authored Copilot-specific guidance. `scripts/syncAgentConfigs.ts` prepends the AGENTS.md content to this addendum and writes the combined result to `.agents/copilot.md`.

## Copilot-specific notes

- Copilot's primary instruction surface is `.github/copilot-instructions.md`. The `.agents/copilot.md` location is for cross-host inventory.
- See [`docs/partners/everything-claude-code.md`](../partners/everything-claude-code.md) for cross-host emit rules.
