# Copy to /etc/atl-mcp-gate.env and chmod 600.
# Do not commit the real password or cookie secret.
GATE_LISTEN_HOST=127.0.0.1
GATE_LISTEN_PORT=3001
GATE_UPSTREAM=http://127.0.0.1:3101
GATE_SESSION_SECONDS=43200
GATE_PASSWORD=change-me
GATE_COOKIE_SECRET=replace-with-at-least-32-random-bytes
